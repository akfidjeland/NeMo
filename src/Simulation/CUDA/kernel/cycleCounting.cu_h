#ifndef CYCLE_COUNTING_CU_H
#define CYCLE_COUNTING_CU_H

#define KERNEL_TIMING

/* Counters are specified using numeric indices. Some care must be taken to
 * ensure that the COUNTER_COUNT specified here matches what's used in the
 * kernel, and furthermore that the counters used there match what's used when
 * reading back. */

#define COUNTER_COUNT 10
#define DURATION_COUNT (COUNTER_COUNT-1)

#endif
